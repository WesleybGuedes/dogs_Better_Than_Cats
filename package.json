{
  "name": "dogs-more-than-cats",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint --no-inline-config --no-error-on-unmatched-pattern -c .eslintrc . --ext .js"
  },
  "devDependencies": {
    "@trybe/eslint-config-frontend": "^1.11.3",
    "vite": "^4.4.5"
  }
}